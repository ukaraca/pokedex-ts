import styled from '@emotion/styled';

const Container = styled.div`
  .nes-container {
    width: 100%;
  }
  .progress-div {
    display: flex;
  }

  .nes-progress {
    height: 1rem;
  }
`;

export default Container;
