import styled from '@emotion/styled';

const Container = styled.div`
  height: 100%;
  .nes-container {
    height: 100%;
  }
  p {
    height: unset;
    min-height: 200px;
  }
`;

export default Container;
